import 'package:flutter/material.dart';

// Dark Theme Colors
const Color primaryColor = Color(0xFF0288F6); // Lighter Sky Blue for contrast
const Color primaryVariantColor = Color(0xFF42A5F5); // Sky Blue
const Color secondaryColor = Color(0xFFA5D6A7); // Light Green
const Color secondaryVariantColor = Color(0xFF66BB6A); // Soft Green
const Color backgroundColor =
    Color(0xFF121212); // Dark Grey (standard dark theme background)
const Color surfaceColor =
    Color(0xFF1E1E1E); // Slightly lighter grey for surfaces
const Color errorColor = Color(0xFFCF6679); // Soft Pinkish Red for errors
const Color onPrimaryColor = Color(0xFF000000); // Black (on Primary)
const Color onSecondaryColor = Color(0xFF000000); // Black (on Secondary)
const Color onBackgroundColor =
    Color(0xFFE0E0E0); // Light Grey for text on dark backgrounds
const Color onSurfaceColor =
    Color(0xFFE0E0E0); // Same as onBackground for consistency
const Color onErrorColor = Color(0xFF000000); // Black (on Error)
